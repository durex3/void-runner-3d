# 特效接入与截图验收

更新日期：2026-09-20。以下“当前验收基线”为现行实现；后面的迭代记录仅用于追溯，旧截图数据与旧方案不代表当前效果。

## 当前验收基线

| 武器 | 当前表现与边界 |
|---|---|
| 骑士重剑 | 世界空间采样实际剑刃，形成淡白色挥砍拖尾；拖尾两端渐细、中段略厚，修复侧对镜头时的可读性 |
| 猎手长弓 | KayKit 实体箭长 1.6，金色拖尾长 2.25、宽 0.52，箭尖亮点 0.23；短促命中闪光和六片碎光，不新增蓄力或改变穿透 |
| 德鲁伊法杖 | 杖尖符文、种子核心与四簇环绕叶片传递到锁定落点；七条根须展开、七簇叶片升起淡出；伤害及减速仍在原固定落点结算 |
| 其他武器 | 保留实体弩箭、轻便手弩残影、工程师序列帧弹体、雷电连接及魔杖核心；资源映射见下方首次接入记录 |

重剑采样窗口为动作进度 0.40–0.59，轨迹保留约 140 毫秒，伤害时刻对齐动作进度 0.52。前摇不显示，收招淡出；拖尾两端通过渐变收尖，中段保持较高亮度，不额外叠加弧线或地面提示。切换武器立即隐藏，独立缓冲和材质随效果结束释放。命中冲击使用集中闪光、碎片与尘土，不以挥空触发命中效果。

### 武器效果验证记录（此前迭代）

- `node tests/heavy-sword-visual.mjs`：本轮通过。桌面 1280×800、手机 390×844 各八方向，检查前摇隐藏、挥砍窗口、命中时序、收招及切换清理。每方向峰值显著变化像素大于 100；实测桌面 275–854、手机 303–955。左下对应场景分别为 345 和 391，截图已人工查看。
- `node tests/ranger-nature-visual.mjs`：上一轮长弓/自然法杖优化时通过桌面与手机端像素、生命周期和浏览器错误检查；本轮重剑修复未改这两类效果。
- `npm run build`：重剑修复后通过，仅有已有的打包体积警告。
- 提交前复验：五个 Node 单测文件共 33 项全部通过，生产构建再次通过；未将本次检查描述为重新运行完整浏览器测试集。
- 截图分别在 `test-results/heavy-sword/`、`test-results/ranger-nature/`；`tests/heavy-sword-showcase.mjs` 为录像工具，旧录像不能代替本轮截图验收。
- `tests/assets-visual.mjs`、`tests/beam-directions.mjs` 为全武器与闪电连接专项脚本；以上视觉脚本均需开发服务运行在 5188，未加入 `npm test` 默认链。

## 本轮敌方危险提示与 Boss 恢复反馈

- 敌弹采用共享几何与材质的深色描边、珊瑚红弹体、浅色核心及方向尾迹；践踏边界固定半径 4，以刻度和收束内环表示范围与剩余预警时间。
- Boss 十方向环射和五发扇射使用与实际弹道一致的方向提示；践踏结算后显示恢复脚环及顶部反击提示。危险通知移至顶部，不再用中央大字遮挡战斗。
- 本轮未修改四职业武器素材与数值，也未引入外部美术资源，来源许可记录无需新增。
- `npm run test:readability` 已通过桌面/手机截图、混战显隐像素差、预判躲避、骑士恢复期反击与暂停等检查；截图在 `test-results/combat-readability/`。另通过 38 项单测、通用浏览器回归及生产构建；上述武器视觉专项未全部重跑。
- 机制与截图检查不替代四职业正常八波试玩或真机性能验收，试玩记录见 [战斗可读性测试](./战斗可读性测试.md)。

## 历史：首次资源接入（下方旧方案已被当前基线替代）

| 武器 | 当前实际资源 |
|---|---|
| 复合弩、轻便手弩 | KayKit arrow_crossbow glTF，包含 rogue_texture 配套贴图；两种尺寸 |
| 长弓 | KayKit arrow_bow glTF，包含 ranger_texture 配套贴图 |
| 炼金霰弹 | StarsteelGaming Fireball 9 帧循环，五发独立飞行 |
| 速射手弩 | StarsteelGaming Thundersphere 8 帧循环，保留三连发机制 |
| 聚能魔杖 | Kenney circle_02 + flare_01 紫色光环与脉动核心；Combat FX 第 12 行命中 |
| 雷鸣法杖 | CraftPix Lightning-bolt 10 帧命中；Kenney spark_06 曲折闪电连接 |
| 德鲁伊法杖 | Combat FX 第 28 行绿色环形爆发；Kenney circle_02 范围提示 |
| 单手剑、双手剑 | Combat FX 第 2 / 25 行，分别为短弧斩与粉红重斩；动画后段才淡出 |
| 钉锤 | Kenney 冲击环、烟尘与 Combat FX 命中，不再叠加剑气 |

修复：箭矢缺贴图、长弓误用冰矛分支、箭头朝向、Warrior 错误切片、整张法术图集误作单帧、弹体材质回收。删除旧的椭圆弹体和施法几何光环；粒子爆发改用贴图。

范围：武器外观使用模型/贴图资源；代码仍负责运动、序列播放、缩放、碰撞和伤害。角色脚圈、状态标记等辅助 UI 仍为几何体，不属于武器美术。

Unholy Ground 原图是诅咒/红色闪电，当前不再作为自然法术直接绘制；Icespear 已保存但不替代长弓。没有把包里的每张图片强行接到技能上。

验证：`tests/assets-visual.mjs` 覆盖 11 套武器、飞行资源标记、动画前后帧、清理与浏览器错误；截图位于 `test-results/assets/`。此外运行机制测试、浏览器流程、骑士与武器测试及生产构建。

来源：KayKit Adventurers；StarsteelGaming Spell Effects（OpenGameArt 页面标注 CC0）；Frostwindz Warrior FREE（本地附带 License Agreement.docx）；CraftPix 10 Magic Effects（本地 License.txt 指向其文件许可）；Kenney Particle Pack（CC0）；Combat FX 1.1（已购）。原始素材均保留在 references/purchased。

## 2026-09-19 截图反馈修正

- 闪电连接改为沿起点到终点的正交基对齐，避免在部分攻击方向横穿地面；贴图朝向摄像机。
- 弓弩箭体缩细、长度收敛，保留三种配色的贴图拖尾。
- 魔杖对 Kenney flare_01 的中心做 UV 取景，补足能量核心；原图未改写。
- 单手剑改为冷白色序列，并叠加 Kenney slash_03 刀刃光；按屏幕宽高比修正刀光朝向。
- 工程师武器效果不变。
- 新增 `node tests/beam-directions.mjs`：验证八方向（含高度差）连接端点误差小于 1e-6，并截图到 `test-results/beam-directions/`。
- 完整武器截图仍在 `test-results/assets/`，包含放大和正常游戏视距。

## 游侠与自然法杖二次优化

- 游侠箭体按模型包围盒归一化尺寸；长弓为长箭、金色长尾，复合弩为粗短箭、青色短尾，手弩为小箭与两段模型残影。拖尾朝向摄像机，裁去贴图左右空白，提高正常视距可见度。克隆材质随弹体回收。
- 三种箭命中尺寸、颜色和持续时间不同；武器射速、伤害、穿透不变。长弓没有新增蓄力机制。
- 自然法杖增加 Kenney magic_01 杖尖符文、Nature Kit grass_leafs 叶簇传递与落点绽开。保持 0.25 秒固定落点伤害及减速；飞行叶簇仅作视觉提示，不能追踪移动目标。
- 新资源均为 Kenney CC0。叶簇是原包 Side 目录的低分辨率 PNG，作为短暂特效使用；未声称是藤蔓模型。
- 专项测试：`node tests/ranger-nature-visual.mjs`，正常视距飞行/命中截图在 `test-results/ranger-nature/`，覆盖资源出现、自然传递与绽开、回收和控制台错误。

## 骑士重剑下劈优化

- 重剑移除 Combat FX 第 25 行粉红大弧光，改用 Kenney slash_04 钢白刀光，按摄像机投影后的下劈方向播放，0.22 秒内移动收束并消散。
- 命中使用 Combat FX heavy 冲击贴图，持续 0.16 秒；少量 Kenney 尘土和地面冲击集中到最近的实际命中目标，挥空只出现刀光。
- 保留原有重剑 60 毫秒局部顿帧、受击和击退；伤害、攻击前摇及范围不改动。刀光是与下劈匹配的视觉轨迹，并非逐帧采样剑刃骨骼。
- 骑士机制与特效测试通过；专项 `node tests/heavy-sword-visual.mjs` 覆盖四方向截图、刀光移动及消散、挥空不产生命中尘土和资源清理，截图在 `test-results/heavy-sword/`。

## 重剑真实剑刃轨迹

后续修订替代预设下劈曲线：从当前挂载重剑的局部包围盒提取剑刃端点，随骨骼动画采样世界坐标，以 Kenney slash_04 贴图绘制短残影。最多保留 9 个采样点及 85 毫秒轨迹，整体 0.22 秒淡出；切换武器立即隐藏，结束时释放材质和几何缓冲。命中冲击仍由实际伤害事件触发，伤害时刻及判定范围不变。

## 修复重剑刀光不可见

上一版历史轨迹面在生成时没有面积，命中顿帧期间无法展开，导致实际游戏几乎看不见。现已替换为跟随剑刃位置和投影方向的 Kenney 贴图 Sprite，保持面向摄像机及最低可见宽度，前 32% 生命周期保持亮度以覆盖 60 毫秒顿帧。该效果为剑刃附着刀光，不再宣称历史轨迹残影。

专项测试增加实际渲染像素差检查：四方向在 1280×800 正常视距下，刀光产生 200–329 个显著变化像素，模拟命中冻结 60 毫秒后仍有 199–323 个；已人工查看截图。骑士特效测试和构建通过。

重剑拖尾收口：移除额外程序化弧线，仅保留真实剑刃采样轨迹；拖尾颜色改为淡白，沿轨迹两端渐细、中段略厚，保留命中冲击与顿帧。
