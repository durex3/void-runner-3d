# 骑士升级与测试说明

更新：2026-09-24。以下是当前实现，第三关仍为设计草案。

## 三种升级

| 升级 | 触发与收益 | 可见反馈 |
|---|---|---|
| 守卫反击 | 持剑盾受到实际伤害，3 秒内下一次命中的剑盾攻击伤害 +50%；空挥不消耗，重复受伤刷新时间 | 角色冷白发光、反击就绪倒计时；命中时白光及“反击” |
| 破阵重斩 | 重剑同次命中至少两人，伤害 +20%、击退 +35% | 整次攻击一道金色斩光、各目标小火星及“破阵” |
| 震击追猎 | 钉锤攻击原本已有硬直的目标时伤害 +35%；不计本次新施加的硬直，Boss 已承诺免控阶段不触发 | 中心青白短闪、向外散开的细碎裂片及“追猎” |

骑士正常升级时优先推荐当前武器未解锁的专属卡；每卡每局一次。属性面板显示已选效果，报告记录升级选择。角色发光使用实例材质，结束/重开还原，避免污染其他角色。条件增伤不纳入未触发时的基础伤害数值。

钉锤现参数：伤害 52、距离 3、间隔 0.8 秒、前摇 `0.25 × 0.8 / 0.9` 秒、普通敌人硬直 0.45 秒。升级没有增加硬直时长。

## 一键预览

运行 `npm run dev` 后访问 <http://127.0.0.1:5188/knight-preview.html>。自动解锁三卡并创建不主动攻击的高生命目标；按钮可切换、重播、循环、暂停，命中通过真实战斗系统结算。追猎使用 2 倍攻速连续两锤，第二锤触发。此页近景方便观察，不代表正常关卡的镜头和难度。手机控制区在底部，不写正式纪录。

当前视觉基线：破阵斩光持续 0.22 秒，只在整次攻击的命中中心生成一次；各目标保留三颗小火星。追猎中心闪光在 0.085 秒内消退，每个强化命中目标产生八片错落飞散的裂片，整体在 0.42 秒内消退。旧版每目标两道宽斩痕、五向相连星形已替换，减少敌人遮挡。基础重剑淡白拖尾仍保留原 140 毫秒寿命。

攻击结束闪剑修复：停止攻击动画会立即恢复骨骼初始姿态，现于同一帧应用待机动画，避免武器在初始手部位置闪现。用户已确认问题消失；没有通过隐藏武器或缩短拖尾规避。

预览回归在正常游戏镜头隐藏文字，比较主体显示/隐藏的画布像素，并保存 `heavySweep-no-text.png`、`macePursuit-no-text.png`；另检查攻击结束帧再次零时间更新动画时，武器世界矩阵不再变化。正常战斗镜头下的人眼可读性仍须与自动触发测试区分。

## 波次与统计

- `/wave-test.html?wave=5-7`：连续第五至第七波；也可选单波。波间恢复 12 生命，第七波后结束；保留地火、精英奖励选择，但关闭升级和装置/伙伴更新。
- 第五波三组，每组 4 近战、2 突进、1 远程，组间停止出怪 3.5 秒。
- 第六波三组，每组 4 近战、3 错峰侧翼突进、1 远程，另保留原精英。第七波暂保持 15 人、5 秒开场缓冲。
- 导出记录最终倍率、各武器攻击时攻速范围；近战弹体数为 0，Boss 伤害来源正常传递。波次测试的 `testContext` 记录范围与无敌开关。
- `hits` 是目标命中次数，可大于攻击次数，不能直接作为群攻命中率。

## 验证命令

`node --test tests/combat.test.mjs`、`node tests/knight-upgrades-browser.mjs`、`node tests/knight-preview-browser.mjs`、`node tests/wave-test-browser.mjs`、`npm run build`。

前三项分别覆盖条件增伤/取消/重置、正常升级界面/属性/报告，以及真实预览触发/伤害/循环/暂停/移动布局。浏览器专项不全部包含在默认 `npm test`，截图位于 `test-results/knight-preview/`。自动通过不等同于所有设备的视觉验收。

下一步见 [第三关开发方案](./第三关开发方案.md)。用户计划明天实现，今天只完成特效与文档。

## 2026-09-24 提交前验证

- 65 项机制单测通过：architecture、character-stats、device-status、garden、combat、levels、run-telemetry。旧推荐测试已同步骑士优先守卫反击的现行规则。
- `node tests/knight-upgrades-browser.mjs`、`node tests/knight-preview-browser.mjs` 通过，包含收招同帧姿态回归。
- `node tests/heavy-sword-visual.mjs` 通过桌面/手机各八方向、挥砍像素、命中时序、收招及换武器清理。此前被页面刷新打断的一轮不算通过，本次使用独立端口完整复跑。
- `npm run build` 通过，仅保留已有的大包体积提示。未重跑完整浏览器专项集合；未新增真机性能验收。
