# 战斗可读性与敌人攻击基线

日期：2026-09-20。基线之后已修复敌方远程缺少前摇提示、Boss 死亡后践踏结算及手机 HUD 重叠。前一轮可读性修复未改变伤害、弹速和正常冷却配置；本轮新增的 Boss 攻防规则见下方独立章节。

## 直接体验

先运行 `npm run dev`，使用下列地址。正常地址不启用测试模块。

| 场景 | 地址 |
|---|---|
| 密集混战：18 名敌人、9 件装置、9 份资源 | http://127.0.0.1:5188/?test=1&scenario=crowd&hero=Knight |
| 混战叠加 Boss 弹幕和践踏 | http://127.0.0.1:5188/?test=1&scenario=mixed&hero=Knight |
| 单个远程敌人练习 | http://127.0.0.1:5188/?test=1&scenario=ranged&hero=Ranger |
| Boss 环形与扇形弹幕练习 | http://127.0.0.1:5188/?test=1&scenario=boss&hero=Ranger |
| Boss 践踏练习 | http://127.0.0.1:5188/?test=1&scenario=stomp&hero=Ranger |

`hero` 可选 Knight、Ranger、Druid、Engineer。骑士与游侠默认第二槽，其他角色默认第一槽；沿用游戏现有移动、切武器、冲刺与暂停操作。刷新或死亡重开恢复固定场景，不保存练习成绩。

测试场景不推进正常波次，也不额外刷怪；单敌练习固定敌人站位、关闭玩家自动攻击，混战保留真实攻击和伤害。初始攻击计时经过安排以便快速复现：远程/Boss 首次发射在 1 秒，践踏首次预警在 1 秒。后续攻击间隔和践踏周期使用原逻辑。这些是测试条件，不代表正式波次难度。

## 自动检查

启动开发服务后运行 `npm run test:readability`。需要本机 Edge，沿用 Playwright 与软件 WebGL 配置。

- 桌面 1280×800、手机尺寸 390×844，各覆盖四角色混战截图及非空画布检查。
- 固定随机种子和初始布置，60 Hz 手动推进真实游戏逻辑，重复场景核对事件时序。加 `manual=1` 后可通过 `__game.lab.step(帧数)` 推进，自动渲染循环不再推进测试。
- 单敌场景验证站立受击、横移躲避、冲刺躲避；死亡敌人不发射，硬直会延后发射。
- Boss 验证十发弹幕、践踏延迟、半径 4 内外边界、步行离圈、暂停冻结、玩家死亡冻结及重开清理。
- Boss 在预警中死亡后断言生命不减少、预警清空；保留一只远处普通敌人，避免以结束整场战斗掩盖结果。
- 远程敌人 0.45 秒、Boss 弹幕 0.6 秒的独立发射前提示，验证暂停冻结、瞄准方向锁定和实际发射时间；硬直中断后重新预警。
- 手机宽度 320、390、700 下检查主 HUD、Boss 血条、危险提示、装置栏互不重叠，覆盖装置栏收起和展开。
- 四角色混战检查隐藏/显示敌弹和践踏圈的像素差异，确认危险效果实际出现在画布上；危险文字保持在顶部区域，取消后不残留。
- 普通首页不暴露 `__game`；测试模块仅由同时带 `test` 和 `scenario` 的地址动态加载。

结果在 `test-results/combat-readability/`：`report.json` 记录发射、受伤、预警和结算时间；PNG 是固定时刻截图；`videos/` 保存浏览器录像，末段为 3.5 秒自由运行混战，其余部分包含手动推进，不能当成全程正常速度录像。画布检测只证明场景有渲染内容，不证明预警一定清楚。

## 修复前基线发现

| 优先级 | 证据 | 下一步建议 |
|---|---|---|
| 高 | 远程敌人和 Boss 在 `fireEnemyProjectiles` 内同时踢攻击动作、生成弹体，没有独立发射前提示。7 单位距离的固定远程测试在 t=1 发射、t≈2.267 受伤；这 1.27 秒是飞行时间，不是前摇 | 先设计清晰且与发射方向一致的出手提示，短距离也需单独验证 |
| 高 | Boss 在 t=1 开始践踏预警，t=1.5 被击杀，预警仍于 t≈2.617 结算并造成 28 伤害 | 明确死亡是否取消已开始的践踏；若保留，需有能解释持续危险的视觉表现 |
| 中 | 手机混战截图中 Boss 血条与装置栏重叠，中央践踏文字压住部分敌人；红色践踏圈在装置环和敌群中辨识度下降 | 优先整理 HUD 位置与危险提示层级，再做混战前后对比 |

独立践踏场景中圈可见，步行可离开；这不意味着混战中的提示已验收合格。本轮查看了桌面和手机截图，仍需玩家以正常速度操作确认反应负担。

## 修复结果

- 远程敌人脚下显示收束环和方向箭头，Boss 显示十方向提示。预警期间停止追击并锁定瞄准，结束后发射；暂停冻结，硬直打断，死亡清除。
- 践踏记录施法 Boss，死亡立即撤掉未结算红圈；即使施法者从敌人列表移除也不会继续造成伤害或摧毁装置。已飞出的弹丸不受影响。
- 手机 Boss 血条、提示、装置栏按顺序排列在主 HUD 下方，随 HUD 高度变化定位，展开内容可滚动。
- 中央大字改为顶部紧凑提示。践踏警告优先于普通消息，并随实际预警清除，不再保留过期危险文字。
- 敌弹使用独立的深色描边、珊瑚红弹体、浅色亮芯和短方向尾迹，不改我方武器素材。践踏采用固定半径 4 的刻度边界和向内收束的计时环，不再让外边界闪缩；薄线叠加层保证敌群和装置中仍能看见边界，不铺满危险区域。

## Boss 攻防调整

针对远程职业容易无伤绕圈、骑士近身成本偏高的反馈，本轮只调整 Boss，不修改四职业武器、普通波次和掉落数值。

- 交替十方向环形弹幕与五发定向扇射，均提前 0.6 秒锁定方向；扇射预判 0.7 秒步行，速度 7.5，伤害仍为每发 12。
- 践踏改为预判远处玩家 1.3 秒移动路线，近身直接锁定当前位置；维持 1.6 秒预警、半径 4 和伤害 28。预警后落点固定，不追踪也不瞬移命中。
- 地面攻击期间停止追击和接触伤害，结算后提供 2.2 秒恢复期并清除 Boss 自身残留弹丸，普通怪的弹丸不清除。恢复期有浅色脚环与顶部反击提示。
- 专项回归覆盖三个远程职业沿原路线受击/变向躲避、环形/扇射交替、锁定方向一致、骑士恢复期近战命中且无 Boss 接触伤害、暂停冻结恢复期及结束后恢复攻击。
- 新截图：`desktop/mobile-boss-fan-warning.png`、`desktop/mobile-boss-recovery.png`。这些是机制验证，不代表已完成四职业多局难度验收；仍需实际游玩反馈决定后续参数。

## 验证边界

本轮已通过 `npm test`（38 项单元测试及通用浏览器回归）、`npm run test:readability`（桌面/手机专项）和 `npm run build`；构建仍有 Three.js 分包超过 500 kB 的提示。完整武器专项没有在本轮全部重跑。

软件 WebGL 和手机尺寸不代表真机帧率或触屏体验；固定场景不等于四角色正常八波平衡测试。粒子等纯视觉随机细节不保证逐像素一致，确定性断言针对游戏事件时序。本轮不声称覆盖所有武器、所有距离或设备。

原有 `npm test` 在与专项浏览器测试同时运行时曾于固定等待后的击杀数断言失败，单独重跑通过；运行这些软件渲染浏览器测试时建议逐个执行。

## 四职业实战复测（待完成）

使用普通入口 http://127.0.0.1:5188/，不带测试参数。四职业各打一局正常速度八波，按平时打法使用各自常用武器，尽量保持升级思路相近，不刻意追求无伤；失败也记录，不要求反复重打直到通关。每局发结算截图并补充以下信息：

| 角色 | 常用武器与主要升级 | 通关/失败、时长及等级 | 普通波次压力与掉血来源 | Boss 躲避与反击体验 |
|---|---|---|---|---|
| 骑士 | 待记录 | 待记录 | 待记录 | 能否利用践踏后的恢复期近身反击 |
| 游侠 | 待记录 | 待记录 | 待记录 | 是否需要变向躲避，预警是否清楚 |
| 德鲁伊 | 待记录 | 待记录 | 待记录 | 是否需要变向躲避，预警是否清楚 |
| 工程师 | 待记录 | 待记录 | 待记录 | 是否需要变向躲避，预警是否清楚 |

每职业一局只作初步对比，不能排除随机升级与操作差异；是否需要普通波次调整，以反馈和追加复测决定。现有结算的装置击杀数不能推导装置伤害占比，尚未实现完整受伤来源与伤害归因统计。
