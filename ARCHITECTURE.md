# 项目架构

当前入口采用“组合根 + 领域模块 + 基础设施适配器”的组织方式。`src/main.js` 只负责创建游戏并处理顶层启动失败；对象组装集中在 `RelicWorkshopGame`，避免业务模块自行查找全局对象。

## 模块职责

| 模块 | 职责 |
|---|---|
| `game.js` | 单局流程协调：生命周期、波次、敌人推进、掉落与各系统调用顺序 |
| `levels.js` | 关卡目录、熔炉波次与纯逻辑地火状态机，供表现和伤害共用 |
| `furnace-world.js` | KayKit 地牢静态实例化与炉栅预警/火焰表现 |
| `furnace-combat.js` | 黑骑士与突击兵锁定方向的预警、执行、收招状态机及扫掠判定 |
| `level-runtime.js` | 统一关卡生命周期接口：`reset`、`beginWave`、`spawnEnemy`、`step`、`cleanup`、`getHudState`；各关卡只持有自己的波次、机制和 HUD |
| `boss-damage-access.js` | 数据驱动的 Boss 普通输出、反制收招倍率、阶段无敌、核心视觉和 HUD；不依赖具体 Boss 技能 |
| `outpost-world.js` / `outpost-combat.js` / `wind-field.js` | 第三关灰盒场景、Clanker 风场技能、风道状态机与边界约束 |
| `combat.js` | 玩家武器攻击模拟，不直接操作界面和输入 |
| `garden.js` | 装置、联动升级、伙伴与 Boss 践踏 |
| `knight-vfx.js` | 骑士专属升级就绪/命中表现，消费真实强化事件，通过 EffectsSystem 管理生命周期与材质还原 |
| `knight-preview.js` | 独立骑士预览，使用真实战斗结算，隔离正式存档；生产入口为 knight-preview.html |
| `actors.js` | 模型加载、装备挂载、骨骼动画和角色清理 |
| `ui.js` | DOM 模板、HUD 和弹窗渲染；通过回调向游戏层报告用户操作 |
| `input-controller.js` | 键盘、左侧虚拟摇杆、触屏冲刺及失焦输入，向游戏层发送归一化方向、模拟力度和动作意图 |
| `effects.js` | 粒子、临时效果和尸体生命周期 |
| `boss-combat.js` | Boss 环形/扇射方向、地面落点预测、符柱反制目标与恢复期参数，供战斗与预警共用 |
| `danger-vfx.js` | 敌弹描边、亮芯及方向尾迹，践踏固定边界与计时环；共享几何和材质，仅负责危险视觉，不参与判定 |
| `melee-vfx.js` | 骑士近战反馈、重剑世界空间剑刃采样、剑尖亮边、程序化彗星月牙弧刃与命中分层反馈，不参与伤害结算 |
| `remote-vfx.js` | 远程发射和命中反馈、自然种子/叶簇传递及根须展开，只消费战斗事件 |
| `projectiles.js` | 箭矢模型、序列帧弹体、面向相机的拖尾和闪电连接，提供 update/dispose 生命周期钩子 |
| `combat-lab.js` | 仅 test + scenario 参数启用的固定战斗场景、手动步进及事件记录，不修改正式规则 |
| `performance-test.js` | 独立性能压测页的五档可重复场景、RAF 帧采样、Three.js renderer 指标和活动对象统计，不进入正式首页 |
| `scene-runtime.js` | 创建渲染器与场景，并对触屏设备按持续帧时间自适应像素比 |
| `audio.js` | Web Audio 总线、静音/暂停生命周期、owner 清理及武器与装置差异化合成音色 |
| `scene-runtime.js` | Three.js 场景、相机、渲染器和基础灯光 |
| `game-state.js` | 局内状态的创建与重置规则 |
| `character-stats.js` | 当前角色、装备与升级后的展示数值计算 |
| `device-status.js` | 装置数量、启动/到期状态、伙伴与联动汇总 |
| `run-telemetry.js` | 单局构筑平衡统计的旁路聚合与版本化报告，不修改战斗规则 |
| `balance-report.js` | 独立构筑平衡分析页：报告指纹去重、本地持久化、单局备注/删除、数据集导入导出、筛选聚合和异常信号，不依赖 Three.js 游戏运行时 |
| `upgrades.js` | 升级候选生成及升级命令 |

## 依赖约束

- `main.js` 可以依赖游戏协调器，业务模块不能反向依赖入口。
- 战斗和装置模块通过构造参数获取场景、状态、伤害回调等依赖，不直接访问 DOM 或全局变量。
- 战斗模块在近战判定完成时通过回调报告命中位置和命中对象；近战特效模块只消费位置结果，游戏协调器据命中对象触发局部停顿、受击动作、音频和镜头反馈。表现层不能反向修改伤害、击退、硬直、冷却或全局计时。
- UI 不修改游戏规则，只展示传入状态并触发回调。
- 新增纯规则时优先写成无 DOM、无计时器的函数或类，以便使用 Node 单测。
- 只有跨多个系统的流程才放进 `game.js`；单一职责逻辑应进入对应模块。
- `RunTelemetry` 只接受战斗和成长边界的实际结果，按武器/装置聚合，不保存逐帧事件；每次重置生成新的 `runId` 供报告去重，伤害使用 `Math.min(enemy.hp, damage)` 的实际扣血值，暂停期间不累计武器持有时间。

升级选项采用数据驱动的命令对象，每个选项封装自己的 `apply` 行为；游戏协调器只负责展示和执行选择。输入、音频、UI、特效通过依赖注入组合，没有引入全局事件总线，避免追踪困难的隐式调用。

## 验证

关卡切换通过 `level-runtime.js` 的统一生命周期边界完成；`game.js` 不再为第三关新增波次分支。Boss 生成后由 `BossDamageAccess` 挂载对应反制策略，主循环调用 `step`，伤害入口调用 `evaluate`，清理路径统一调用 `reset`。普通躲避不会自动获得长收招，技能模块只在符柱、炉栅或风眼核心反制成功时设置恢复状态。暂停不推进反制、收招和风道计时。重开、退出、死亡和胜负会移除视觉并清空引用。只在 `?test=1` 暴露手动步进、关卡切换、Boss 状态与渲染接口。

`npm run test:level2` 覆盖第二关纯机制及浏览器流程；正常速度的四职业通关、难度与真机性能仍需人工验收，见 [第二关验收](./第二关验收.md)。

Boss 攻防状态由真实游戏循环推进：`game.js` 记录玩家实际步行速度并锁定扇射方向；`garden.js` 锁定预测践踏落点，结算后设置恢复期并调用注入的 `onBossRecovery`。协调器按弹丸 `source` 仅清理该 Boss 的弹丸。地面预警和恢复期阻止该 Boss 追击、射击计时与接触伤害；暂停冻结推进，死亡或移除施法者取消未结算践踏。`effects.js` 和 `ui.js` 仅展示方向、恢复脚环和提示。

Boss 反制是独立的伤害策略层：近战、远程和装置统一通过 `evaluate` 获得普通/破防收招倍率；视觉和 HUD 只消费 `getHudState`，不自行重复判定。白色球形护罩只由反制成功事件触发短闪，通用收招标记不再显示护罩。压缩空间技能只影响临时走位与反制条件，不把玩家推出边界，也不改变敌人、敌弹和装置的碰撞。以后可增加视线、破盾、节点或时间窗口策略，而无需扩展 `game.js` 的关卡条件树。

战斗实验入口通过 `exposeTestApi` 动态安装，仅显式测试场景包装游戏实例的方法。手动模式在 RAF 中跳过推进，由实验入口逐帧调用真实 tick/effects/camera；自由运行模式使用原游戏循环。随机数替换只在同步初始化和 tick 内生效，并通过 finally 恢复。具体测试条件与限制见 [战斗可读性测试](./战斗可读性测试.md)。

性能压测入口为独立 `performance-test.html`，由 `main.js` 按路径动态加载 `performance-test.js`。它复用真实第二关、敌人、Boss、装置、弹丸、地火和特效系统，仅替换波次推进、升级与纪录写入，确保预设可以重复运行。页面暴露 `__THREE_GAME_DIAGNOSTICS__`，读取 renderer.info、视口/DPR 和活动对象数量；RAF 采样只排除隐藏页和超过 2 秒的休眠帧，低端设备的真实慢帧会保留。

触屏设备由 `scene-runtime.js` 根据帧时间 EMA 调整 DPR：高于 20.5 ms 持续 1 秒时每次降低 0.1，最低 1.25；低于 17 ms 持续 3 秒时逐步恢复，整局重开时恢复初始 DPR。普通鼠标/桌面不启用此策略。性能页 15 秒 benchmark 使用独立窗口，结束后冻结结果；装置电击路径以单个折线替代多段圆柱 Draw Calls，特效池满载时移动端降低非关键碎光数量。

2026-09-20 特效生命周期：启动时等待近战、远程、弹体、通用粒子和装置贴图预加载。`combat.js` 推进弹体视觉更新，在到期和重置时调用 `dispose`；敌方弹体由 `game.js` 同样回收。共享模型/贴图不随单发销毁，克隆材质和独立几何体随对象清理。

重剑在开火时注册跟随骨骼动画的轨迹，主要挥砍窗口为动作进度 0.40–0.59，命中对齐进度 0.52，历史采样保留约 110 毫秒。原有世界空间剑刃拖尾继续保留，并叠加独立的程序化彗星月牙弧刃：弧刃在动作进度约 0.28–0.72 显示，两端收尖、中段加厚，由金色外焰、白色核心和淡金残影组成；只消费动画朝向，不修改命中范围。切换武器、角色死亡或动作替换时同时隐藏并回收全部层。自然法术传递仅为表现，不新增追踪弹体或修改锁定落点。

第二关敌方重斩、冲锋和召唤预警仍由 `furnace-combat.js` 的锁定状态机驱动。预警材质使用较低填充透明度和较低 render order，玩家近战弧刃位于更高表现层；这只解决视觉遮挡，判定方向、时长和伤害保持不变。

- `npm test`：架构规则、战斗与装置单测，以及完整浏览器流程。
- `npm run test:readability`：混战像素、危险提示、Boss 环形/扇射与地面预判、近战恢复窗口、暂停及窄屏回归。
- `node tests/heavy-sword-visual.mjs` / `node tests/ranger-nature-visual.mjs`：真实渲染像素、桌面/手机可见性、时序与回收。
- `node tests/assets-visual.mjs` / `node tests/beam-directions.mjs`：资源接入与闪电方向回归。
- `npm run test:weapons`：角色武器、切换、暂停、重开、骑士动作、命中反馈及桌面/移动端特效。
- `npm run test:mobile`：摇杆模拟量、死区、释放归零、冲刺与移动安全区。
- `npm run test:audio`：音频总线、静音、暂停/恢复、切后台和 owner 生命周期。
- `npm run test:performance`：独立压测页五档预设、390×844 指标、Three.js renderer diagnostics 与截图。
- `npm run test:telemetry`：构筑平衡报告单测、独立 Google Chrome 移动尺寸回归、复制与 JSON 下载。
- `npm run test:balance`：构筑分析聚合与数据集单测，以及 Google Chrome 桌面/移动布局、刷新恢复、重复导入去重、备注、删除、筛选和两类 JSON 下载回归。
- `node tests/kaykit.mjs`：角色模型、骨骼、动画与资源清理。
- `npm run build`：生产构建。

## 骑士收招姿态约束

AnimationAction.stop() 会立即还原骨骼初始姿态。分层角色停止攻击或受击动作后，必须在渲染前执行 mixer.update(0)，让替代待机动作在同一帧生效。预览回归通过比较切换帧与额外零时间更新后的武器世界矩阵，防止出现一帧闪剑。
