# 项目架构

当前入口采用“组合根 + 领域模块 + 基础设施适配器”的组织方式。`src/main.js` 只负责创建游戏并处理顶层启动失败；对象组装集中在 `RelicWorkshopGame`，避免业务模块自行查找全局对象。

## 模块职责

| 模块 | 职责 |
|---|---|
| `game.js` | 单局流程协调：生命周期、波次、敌人推进、掉落与各系统调用顺序 |
| `combat.js` | 玩家武器攻击模拟，不直接操作界面和输入 |
| `garden.js` | 装置、联动升级、伙伴与 Boss 践踏 |
| `actors.js` | 模型加载、装备挂载、骨骼动画和角色清理 |
| `ui.js` | DOM 模板、HUD 和弹窗渲染；通过回调向游戏层报告用户操作 |
| `input-controller.js` | 键盘、触屏及失焦输入，向游戏层发送意图 |
| `effects.js` | 粒子、临时效果和尸体生命周期 |
| `melee-vfx.js` | 骑士近战反馈、重剑世界空间剑刃采样、面向镜头的剑尖亮边与淡出，不参与伤害结算 |
| `remote-vfx.js` | 远程发射和命中反馈、自然种子/叶簇传递及根须展开，只消费战斗事件 |
| `projectiles.js` | 箭矢模型、序列帧弹体、面向相机的拖尾和闪电连接，提供 update/dispose 生命周期钩子 |
| `combat-lab.js` | 仅 test + scenario 参数启用的固定战斗场景、手动步进及事件记录，不修改正式规则 |
| `audio.js` | Web Audio 播放、静音状态及武器差异化合成音色 |
| `scene-runtime.js` | Three.js 场景、相机、渲染器和基础灯光 |
| `game-state.js` | 局内状态的创建与重置规则 |
| `character-stats.js` | 当前角色、装备与升级后的展示数值计算 |
| `device-status.js` | 装置数量、启动/到期状态、伙伴与联动汇总 |
| `upgrades.js` | 升级候选生成及升级命令 |

## 依赖约束

- `main.js` 可以依赖游戏协调器，业务模块不能反向依赖入口。
- 战斗和装置模块通过构造参数获取场景、状态、伤害回调等依赖，不直接访问 DOM 或全局变量。
- 战斗模块在近战判定完成时通过回调报告命中位置和命中对象；近战特效模块只消费位置结果，游戏协调器据命中对象触发局部停顿、受击动作、音频和镜头反馈。表现层不能反向修改伤害、击退、硬直、冷却或全局计时。
- UI 不修改游戏规则，只展示传入状态并触发回调。
- 新增纯规则时优先写成无 DOM、无计时器的函数或类，以便使用 Node 单测。
- 只有跨多个系统的流程才放进 `game.js`；单一职责逻辑应进入对应模块。

升级选项采用数据驱动的命令对象，每个选项封装自己的 `apply` 行为；游戏协调器只负责展示和执行选择。输入、音频、UI、特效通过依赖注入组合，没有引入全局事件总线，避免追踪困难的隐式调用。

## 验证

战斗实验入口通过 `exposeTestApi` 动态安装，仅显式测试场景包装游戏实例的方法。手动模式在 RAF 中跳过推进，由实验入口逐帧调用真实 tick/effects/camera；自由运行模式使用原游戏循环。随机数替换只在同步初始化和 tick 内生效，并通过 finally 恢复。具体测试条件与限制见 [战斗可读性测试](./战斗可读性测试.md)。

2026-09-20 特效生命周期：启动时等待近战、远程、弹体、通用粒子和装置贴图预加载。`combat.js` 推进弹体视觉更新，在到期和重置时调用 `dispose`；敌方弹体由 `game.js` 同样回收。共享模型/贴图不随单发销毁，克隆材质和独立几何体随对象清理。

重剑在开火时注册跟随骨骼动画的轨迹，主要挥砍窗口为动作进度 0.40–0.59，命中对齐进度 0.52，历史采样保留约 75 毫秒。原拖尾保留深度关系，仅窄剑尖亮边关闭深度测试以保证斜向可见；切换武器同时隐藏两层并回收。自然法术传递仅为表现，不新增追踪弹体或修改锁定落点。

- `npm test`：架构规则、战斗与装置单测，以及完整浏览器流程。
- `node tests/heavy-sword-visual.mjs` / `node tests/ranger-nature-visual.mjs`：真实渲染像素、桌面/手机可见性、时序与回收。
- `node tests/assets-visual.mjs` / `node tests/beam-directions.mjs`：资源接入与闪电方向回归。
- `npm run test:weapons`：角色武器、切换、暂停、重开、骑士动作、命中反馈及桌面/移动端特效。
- `node tests/kaykit.mjs`：角色模型、骨骼、动画与资源清理。
- `npm run build`：生产构建。
